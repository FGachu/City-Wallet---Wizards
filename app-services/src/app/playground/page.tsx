import { PlaygroundClient } from "./PlaygroundClient";

export const metadata = {
  title: "API Playground · City Wallet",
};

export default function PlaygroundPage() {
  return <PlaygroundClient />;
}
